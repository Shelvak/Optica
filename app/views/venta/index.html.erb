<h1>Ventas</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th> Mes </th>
      <th> Año </th>
      <th colspan="3"> Flotantes </th>
      <th colspan="3"> Contacto </th>
      <th colspan="3"> Total (con otros) </th>
    </tr>
  </thead>

  <tbody>
    <% @venta.each do |ventum| %>
      <tr>
        <td><%= ventum.month %></td>
        <td><%= ventum.year %></td>
        <td></td>
        <td><%= ventum.floating_quantity %></td>
        <td><%= number_to_currency(ventum.floating_amount) %></td>
        <td></td>
        <td><%= ventum.contact_quantity %></td>
        <td><%= number_to_currency(ventum.contact_amount) %></td>
        <td></td>
        <td><%= ventum.total_quantity %></td>
        <td><%= number_to_currency(ventum.total_amount) %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

<br />
